import {
    BackDiv,
    DdayTitle,
}
from "../../Presenter/UserDday/UseDdayPresent";

const UserDday = () => {

    
    return (
        <>
            <BackDiv>
                <DdayTitle>디데이 설정</DdayTitle>
            </BackDiv>
        </>
    );
};

export default UserDday;